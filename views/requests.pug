extends base

block content
    div
        h2 Restock Requests
        h5 Total:
            a(href="#") Test12Test
    div.mt-5
        h2 Quote Request
        h5 Total: 
            a(href="/requestedquotes") #{count}